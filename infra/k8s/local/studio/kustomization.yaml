apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: stack-studio
# O Secret `studio-secrets` NAO entra aqui: e criado pelo deploy.sh com os
# valores reais do .env (mesma regra do stack-knowledge-base).
resources:
  - namespace.yaml
  - limits.yaml
  - postgres.yaml
  - studio-api.yaml
  - studio-ui.yaml
